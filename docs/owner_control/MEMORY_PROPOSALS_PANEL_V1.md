# Memory Proposals Panel V1

The memory proposals panel is review-only.

It can show proposed memory cards, source summaries, confidence, sensitivity class, and owner decision states. It cannot write memory directly and cannot auto-commit memory.

Direct memory write and memory auto-write remain denied. Curator decision is required in a future stage.
