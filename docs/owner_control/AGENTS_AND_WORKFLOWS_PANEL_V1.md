# Agents And Workflows Panel V1

The agents/workflows panel shows local synthetic status for agents, queues, workflow steps, incidents, and evidence.

It is a status surface only. It does not enable VoltAgent runtime, external MCP servers, browser automation, production workflows, or production writes.

Each PASS status requires evidence. False PASS without evidence is rejected by the owner-control validator.
