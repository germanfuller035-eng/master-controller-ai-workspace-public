# Feature Flags Panel V1

The feature flags panel is display-only.

It shows OFF, LOCAL_SYNTHETIC, and future lifecycle states from the registry. It cannot enable production feature flags or change flag lifecycle.

Production deploy, outbound email, outbound social, auto safe, voice, browser actions, payments, and production DB write remain OFF.
