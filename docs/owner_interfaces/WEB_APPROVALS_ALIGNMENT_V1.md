# Web Approvals Alignment V1

SESSION=POLICY_SECRETS_AUDIT_EMERGENCY_CONTROL_V1
STATUS=DOC_ONLY_NO_WEB_IMPLEMENTATION

Future web approval panels must bind confirmation to the exact payload hash and expiry. They must show rollback/cancel status and never show secret values.

STOP in web must expose disabled queues, revoked approvals, active residual actions, checkpoint location, and the next safe action.
