<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base app theme (Material 3, dark default handled via -night) -->
    <style name="Theme.MaterController" parent="android:Theme.Material.NoActionBar" />

    <!-- Splash theme -->
    <style name="Theme.MaterController.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.MaterController</item>
    </style>
</resources>
