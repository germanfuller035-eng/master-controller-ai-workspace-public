# Screenshots / UI Hierarchy Index

SESSION_NAME=PRODUCTION_NO_SEND_MASTER_CONTROLLER_V1
SCREENSHOT_COMMITTED=NO
UI_XML_COMMITTED=NO

Reason: the installed app contains private real-lead import data in the owner workflow. Raw screenshots and UI hierarchy XML can expose company/contact/domain values. They were used only transiently for local smoke checks and were not committed.

Committed replacement evidence:

- HONOR_SMOKE_RESULT.json: safe boolean screen/route checks only.
