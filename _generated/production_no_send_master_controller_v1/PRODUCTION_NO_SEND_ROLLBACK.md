# Rollback

SESSION_NAME=PRODUCTION_NO_SEND_MASTER_CONTROLLER_V1

Rollback options:

1. Reinstall the previous accepted APK if owner rejects this build.
2. Revert the final Git commit if the source changes need to be backed out.
3. Remove the app-local private import from the device if the owner wants the Honor app returned to a no-real-leads local state.

No production rollback is required because no production systems were changed.
