# v0.4.0-rc1 — Post-Release Acceptance Addendum

Date: 2026-06-17. Addendum to MASTER_CONTROLLER_v0.4.0-rc1_FINAL_RELEASE_REPORT_2026-06-17.md.
No architecture rebuilt. No sends enabled. No duplicate infrastructure. Tag not moved.

## 1. Tag target reconciliation
Tag v0.4.0-rc1 → 9d346f3. Commits after tag (a7c20ac, 9cb073d) contain ONLY documentation
(bundle convenience-copies of already-tracked reports + checkpoint). `git diff --name-status
9d346f3..HEAD` = 5 doc/bundle files, zero runtime/Android/backend/test/migration.
TAG_TARGET_CORRECT=YES · HEAD_AFTER_TAG_CONTENT=docs-only · TAG_MOVE_REQUIRED=NO.

## 2. HEAD-after-tag explanation
HEAD (9cb073d) = tag (9d346f3) + 2 post-release doc commits. Expected and correct: the tag marks
the releasable source+metadata; later docs are post-release record-keeping.

## 3. Artifact source match
BUILD_INFO records gitCommit=ef6eaf3 (the clean-build commit). Android source tree is BYTE-IDENTICAL
between ef6eaf3 and tag target 9d346f3 (`git diff ef6eaf3 9d346f3 -- app/src build.gradle.kts` = empty).
Only reports/artifacts differ between those commits. No rebuild required.
ARTIFACT_SOURCE_MATCH=YES · BUILD_INFO_MATCH=YES (source-identical) · SHA256_MATCH=YES (live = manifest).

## 4. Send-count definitions (one unambiguous meaning each)
- HISTORICAL_CANONICAL_SENDS = 7 (total `result:SENT` rows in outbound_send_ledger.jsonl, all-time).
- HISTORICAL_LEDGER_SUCCESSFUL_SENDS = 7 rows; of which:
  - REAL client send = 1 (lead 002, 2026-06-08 — predates release).
  - TEST_ONLY/selftest/internal-validation = 6 (incl. TEST_KGBI23_E2E on 06-16).
- RELEASE_WINDOW_SENDS (real client) = 0. The only 06-16/17 ledger row is TEST_KGBI23_E2E (a TEST).
- SIMULATED_SENDS = 0 (E2E uses isolated store, never the production ledger).
- SMTP_CALLS_RELEASE_WINDOW = 0. Today (06-17) ledger entries = 0.
Reconciles the earlier "CANONICAL_SENDS=7" (historical) vs "BEFORE/AFTER=0" (release-window, real).
No historical record altered.

## 5. Overpass metric definitions
ONE implementation, layered: worker LEAD_DISCOVERY → Lead Hunter `OverpassAdapter`
(`tools/lead_hunter/src/adapters/index.mjs`, "the single Overpass runtime owner") → audited
`osm_overpass_connector.mjs`. On-demand (no daemon). At observation: 0 active requests, IDLE.
OVERPASS_RUNTIME_IMPLEMENTATIONS=1 · OVERPASS_ACTIVE_REQUESTS_AT_CHECK=0 · OVERPASS_STATE=IDLE ·
DUPLICATE_OVERPASS_IMPLEMENTATIONS=0. Idle ≠ absent.

## 6. Legacy test quarantine status
16 quarantined (see LEGACY_TEST_QUARANTINE_MANIFEST). 13 untracked scratch, 3 tracked (legacy bot,
undeployed). 0 unclassified, 0 regression. NOT run by any CI (no .github, no hooks) — cannot gate
release. Active suites green: API 47/47, api_only 25/25+18/18, Android 66/66, E2E 21/21+17/17.
Not deleted (preserve history).

## 7. Remote publication status
REMOTE_FOUND=NO (`git remote -v` empty; no origin). BRANCH_PUSH=blocked (no destination).
TAG_PUSH=blocked. PR=N/A. Owner must add a remote first; then exact commands:
`git remote add origin <url>` → `git push -u origin feature/master-controller-lead-hunter-integration`
→ `git push origin v0.4.0-rc1`. Did NOT push (rule 15 + nothing to push to).

## 8. Owner Telegram acceptance
Server-side GREEN. Interactive owner taps pending → OWNER_TELEGRAM_SMOKE=BLOCKED_OWNER_INTERACTION.
Checklist: OWNER_ACCEPTANCE_CHECKLIST_v0.4.0-rc1_2026-06-17.md. Does not invalidate server-side proof.

## 9. Android physical-device status
`adb devices` = none connected. PHYSICAL_DEVICE_TEST=BLOCKED_DEVICE_NOT_CONNECTED. Install/smoke
procedure + APK hash handed off in the checklist. No PASS claimed without a device (rule 19).

## 10. Soak status
T+0 baseline captured (2026-06-16T23:28Z): api/worker/telegram active, NRestarts=0, single poller,
0 failed units, 0 SMTP listeners, canonical rev 66/50 leads, no-send env intact, RAM ~609MB avail,
disk ~5.7GB avail, load 0.00. Read-only hourly capture installed: `mc-soak-capture.timer` (oneshot,
User=masterctl, appends JSONL) — NOT a scheduler/worker/poller. Survives reboot (Persistent=true).
NO_SEND_SOAK=IN_PROGRESS_WITH_REAL_SCHEDULE. T+1/6/12/24h to be collected from
/opt/master-controller/soak/soak_observations.jsonl (real data only, no fabrication).

## 11. Backup / restore follow-up
Two backups verified: release_20260617_prereboot, release_20260617_postaccept. Restore drill (temp,
non-destructive): checksum match, valid JSON, revision 66 / 50 leads. BACKUP=PASS, RESTORE_DRILL=PASS.

## 12. Remaining limitations (all external / owner)
- OWNER_TELEGRAM_SMOKE: needs owner taps.
- PHYSICAL_DEVICE_TEST: needs a connected Android device.
- Remote publication: needs owner to configure a git remote.
- 24h soak: in progress via real hourly timer; full window completes T+24h.
No code/architecture blocker. Safety held: AUTOSEND=BLOCKED, SEND_ALLOWED_LIVE=OFF, sends=0, SMTP=0.
