# Context Pack — Master Controller (release v0.4.0-rc1)

- project_id: master_controller
- agent: claude
- task: final validation
- generated_ts: 20260617_025903
- status: ACCEPTANCE · priority: P0_CRITICAL · production: PRODUCTION_SOAK
- branch: feature/master-controller-lead-hunter-integration · release: v0.4.0-rc1
- source_of_truth: VPS Master Controller API + canonical JSON store (13_sales/lead_pipeline_store.json)

## Goal & current verified status
- Next major action: Complete 24h no-send soak; owner acceptance; then controlled production prep
- Risk: high
- Blockers: 24h soak in progress (FREEZE)

## Architecture constraints
- VPS is the sole canonical writer. One send seam, one ledger, one store.
- Telegram + Android are API-only. Single Overpass implementation.

## Files that must NOT be changed
- Anything under tools/telegram_gateway, tools/mater_controller_api, tools/master_controller, apps/mater_controller_android, dist/mater_controller_android
- 13_sales ledgers; release tag v0.4.0-rc1; soak timer.

## Safety rules
- Do NOT modify the VPS, Master Controller API, canonical store, scheduler, worker, or IMAP.
- Do NOT deploy, restart production services, or reboot.
- Autosend is BLOCKED; client email/Telegram send is OFF. No SMTP calls.
- Do NOT move the release tag v0.4.0-rc1 or rewrite release branch history.
- Secrets live only in D:\AI_SECRETS / gitignored .env. Never read or print secret values.
- No mass orphan cleanup; no bulk file moves or deletes.
- Obsidian = knowledge; VPS = operational truth. No bidirectional sync.

## Relevant sources (curated, secret-free)
### tools/mater_controller_api/README.md  [fresh, 1d]
```
# Mater Controller API

Owner-only HTTP gateway over the existing AI_WORKSPACE **Mini Audit** services.
The API is a thin, secure seam — it reuses the canonical business logic and never
creates a second store, a second ledger, or a second send path. **Autosend is always BLOCKED.**

## Architecture

```
Mater Controller Android
        │  HTTPS / LAN, Bearer device token
        ▼
Mater Controller API  (this service, Node ESM + Express)
        │  in-process import, no duplication
        ▼
tools/telegram_gateway/  shared Mini Audit core
   ├── mini_audit_operator_mode.mjs   getMiniAuditOperatorState, buildFollowupDraft
   ├── queue_navigation.mjs           computeLeadEligibility, resolvePreviewSource
   └── outbound_channel_router.mjs    sendApprovedMessage  (the ONLY send seam)
        ▼
13_sales/lead_pipeline_store.json   (canonical store — atomic, lock-guarded)
13_sales/outbound_send_ledger.jsonl (canonical ledger)
```

The same shared modules are used by the Telegram Master Controller, so both
interfaces share one source of truth and one eligibility gate.

## Requirements

- Node.js (ESM). `express` resolves from the workspace `node_modules`.
- The API secret is generated on first run into `D:\AI_SECRETS\01_env\mater_controller_api.env`
  and is **never** printed or copied into the workspace.

## Start / stop / check

```powershell
# loopback only (default, safest)
.\start_mater_controller_api.ps1

# private LAN (binds 0.0.0.0; pair a phone over Wi-Fi) — needs the firewall rule below
.\start_mater_controller_api.ps1 -Lan -Port 8787

# no-send test mode (no real SMTP eve
```

### 00_MASTER_CONTROLLER/MASTER_CONTROLLER_README.md  [STALE, 16d]
> ⚠️ STALE source — verify before relying on it.
```
# MASTER_CONTROLLER_V0.1

## 1. Назначение

Master Controller v0.1 — единый файловый командный центр AI_WORKSPACE.

Он принимает короткие команды Дмитрия, классифицирует задачу, выбирает нужный pipeline, формирует task packet для Cline, фиксирует approval gates и обновляет dashboard.

## 2. Что он делает

- принимает команды;
- классифицирует задачи;
- выбирает agent / pipeline;
- создаёт task packet;
- обновляет dashboard;
- ведёт лог;
- показывает next actions;
- контролирует approval gates;
- не даёт агентам выполнять опасные действия без Дмитрия.

## 3. Что он НЕ делает

- не отправляет сообщения клиентам;
- не создаёт клиентский PDF без approval;
- не меняет цены;
- не отправляет КП;
- не логинится;
- не работает с паролями;
- не удаляет файлы;
- не запускает платные сервисы;
- не совершает юридически значимые действия.

## 4. Главная схема

Dmitry Command
→ current_command.md
→ Task Classifier
→ Command Router
→ Pipeline Router
→ Approval Gatekeeper
→ Cline Task Packet
→ Dashboard Update
→ Decision Brief

## 5. Режим v0.1

Файловый режим.
Без Telegram.
Без API.
Без автоотправки.
Без опасного auto-approve.
## Knowledge Graph Links

- [[MOC_AI_WORKSPACE]]
- [[MOC_TELEGRAM_MASTER_CONTROLLER]]
- [[MOC_BOT_FACTORY_CORE]]
- [[MOC_SECURITY_AND_APPROVAL_GATES]]
- [[MOC_AGENTS]]

Tags: #system/telegram-bot #status/build
```

### tools/master_controller/README.md  [STALE, 28d]
> ⚠️ STALE source — verify before relying on it.
```
# MASTER CONTROLLER RUNTIME

## Назначение

Скрипт читает current_command.md и создаёт task packet / decision brief / next actions.

## Команда запуска

node "D:\AI_WORKSPACE\tools\master_controller\run_master_controller.mjs"

## Где писать команду

00_MASTER_CONTROLLER/inbox/current_command.md

## Что создаётся

- current_task_packet.md
- latest_decision_brief.md
- latest_next_actions.md
- master_controller_log.md
- dashboard_state.json update

## Что runtime НЕ делает

- не отправляет сообщения;
- не создаёт PDF;
- не меняет цены;
- не запускает аудит;
- не запускает поиск;
- не контактирует с клиентами.

## Пример команды

/daily (возвращает реальный дневной бриф)

/status all (возвращает реальный статус системы)

/approval show (возвращает очередь на approval)

/mini_audit EDERA next

/dashboard update
```

### tools/mater_controller_api/package.json  [fresh, 1d]
```
{
  "name": "mater-controller-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Master Controller API — owner-only HTTP gateway over existing AI_WORKSPACE Mini Audit services. No second store, no second ledger, no second send path. (npm name stays mater-controller-api for compatibility.)",
  "main": "src/server/index.mjs",
  "scripts": {
    "start": "node src/server/index.mjs",
    "test": "node tests/run_all.mjs"
  },
  "dependencies": {
    "express": "*"
  }
}
```

### tools/master_controller/run_command.mjs  [STALE, 22d]
> ⚠️ STALE source — verify before relying on it.
```
/**
 * run_command.mjs — Master Controller Command Executor v1.4
 *
 * Usage:
 *   node run_command.mjs "/followup ATOM draft"
 *   node run_command.mjs "/status KGBI"
 *   node run_command.mjs "/lead GSK first_message draft"
 *   node run_command.mjs "/report money"
 *   node run_command.mjs "/inbox"
 *   node run_command.mjs "/execute dashboard"
 *   node run_command.mjs "/execute next"
 *   node run_command.mjs "/contact ATOM set_email info@zavodatom.ru source=https://zavodatom.ru"
 *   node run_command.mjs "/contact GSK confirm_email"
 *   node run_command.mjs "/contact GSK unconfirm_email"
 *   node run_command.mjs "/contact ATOM show"
 *
 * SAFETY:
 *   - Does NOT send to clients
 *   - All outbound messages → outbound_drafts.json only
 *   - Preview sent to Telegram (not clients)
 */

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';
import { spawnSync } from 'child_process';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const WORKSPACE = 'D:\\AI_WORKSPACE';

const DATA = path.join(WORKSPACE, 'data');
const OUTBOUND_DRAFTS = path.join(DATA, 'outbound_drafts.json');
const EVENTS_LOG = path.join(DATA, 'events_log.json');
const ACTION_QUEUE = path.join(DATA, 'action_queue.json');
const DECISION_BRIEF = path.join(WORKSPACE, '00_MASTER_CONTROLLER', 'outbox', 'latest_decision_brief.md');

const now = () => new Date().toISOString();

function readJSON(filePath, fallback = []) {
    try {
        if (!fs.existsSync(filePath)) return fallback;
        c
```

### tools/master_controller/lead_intake_adapter.mjs  [STALE, 23d]
> ⚠️ STALE source — verify before relying on it.
```
/**
 * lead_intake_adapter.mjs
 * Telegram Lead Intake Layer for Mini Audit 10K
 * Safe: read/write CSV only. No email. No client send. No auto-send.
 * Created: 2026-05-25
 */

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const WORKSPACE = path.resolve(__dirname, '../..');

const REAL_CSV_PATH = path.join(WORKSPACE, '13_sales/daily_lead_factory/input/mini_audit_10k_leads_REAL.csv');
const TEMPLATE_CSV_PATH = path.join(WORKSPACE, '13_sales/daily_lead_factory/input/mini_audit_10k_leads_template.csv');
const BACKUP_DIR = path.join(WORKSPACE, '00_BACKUPS/daily_lead_factory');

const CSV_HEADERS = [
  'company_name', 'niche', 'city', 'website_url',
  'contact_phone', 'contact_email', 'whatsapp', 'telegram',
  'source', 'visible_problem', 'business_strength', 'site_weakness',
  'estimated_check', 'decision_maker_hint', 'notes'
];

// Aliases for parsing free-form text
const FIELD_ALIASES = {
  company_name:       ['компания', 'company', 'название', 'организация', 'фирма'],
  website_url:        ['сайт', 'site', 'url', 'ссылка', 'website', 'сайт:'],
  city:               ['город', 'city', 'регион'],
  niche:              ['ниша', 'niche', 'отрасль', 'сфера', 'направление'],
  contact_phone:      ['телефон', 'тел', 'phone', 'номер', 'tel'],
  whatsapp:           ['whatsapp', 'вотсап', 'ватсап', 'wa'],
  contact_email:      ['email', 'e-mail', 'почта', 'mail'],
  telegram:           ['telegram', 'tg', 'телеграм'],
  source:             ['источник
```

### tools/master_controller/run_master_controller.mjs  [STALE, 26d]
> ⚠️ STALE source — verify before relying on it.
```
import fs from 'fs';
import path from 'path';

const WORKSPACE_DIR = 'D:\\AI_WORKSPACE';
const DATA_DIR = path.join(WORKSPACE_DIR, 'data');
const COMMAND_FILE = path.join(WORKSPACE_DIR, '00_MASTER_CONTROLLER', 'inbox', 'current_command.md');
const LOG_FILE = path.join(WORKSPACE_DIR, '00_MASTER_CONTROLLER', 'logs', 'master_controller_log.md');
const DASHBOARD_STATE_FILE = path.join(WORKSPACE_DIR, '09_dashboards', 'dashboard_state.json');

const OUTBOX_DIR = path.join(WORKSPACE_DIR, '00_MASTER_CONTROLLER', 'outbox');
const TASK_PACKET_FILE = path.join(OUTBOX_DIR, 'current_task_packet.md');
const DECISION_BRIEF_FILE = path.join(OUTBOX_DIR, 'latest_decision_brief.md');
const NEXT_ACTIONS_FILE = path.join(OUTBOX_DIR, 'latest_next_actions.md');

// Data Layer v0.2 — canonical files
const DATA_LAYER_FILES = [
    'system_state.json',
    'clients.json',
    'leads.json',
    'deals.json',
    'tasks.json',
    'approvals.json',
    'payments.json',
    'followups.json',
    'events_log.json'
];

// Routing Table
const ROUTING_TABLE = {
    '/status': { route: 'Master Controller State', agent: 'Master Controller Agent', output: 'status brief', approval: false },
    '/dashboard': { route: 'Dashboard Controller Agent', agent: 'Dashboard Controller Agent', output: 'dashboard update task', approval: false },
    '/approval': { route: 'Approval Gatekeeper Agent', agent: 'Approval Gatekeeper Agent', output: 'approval queue brief', approval: false },
    '/lead': { route: 'Lead Generation Pipeline', agent: 'Lead Orchestrator Agent', output: 'lead task pack
```

## Expected output format
- Complete implementation block + tests + commit. Live verification only when explicitly permitted.
