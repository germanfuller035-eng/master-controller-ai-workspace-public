# NEXT SINGLE SEND — Execution Plan (НЕ выполнять в текущей задаче)

**Назначение:** точный порядок будущей одиночной отправки первого касания. Этот документ — план; шлюз отправки НЕ включается. Gate C1C = NOT_ACTIVATED.

## Предусловие (обязательно)
0. **Разрешить блокер `send_uncertain` по BETON-MASTERS_RU**: владелец подтверждает, что попытка 2026-06-12 (`uncertain_no_smtp_proof`) фактически не доставлена. Если доставлена — сменить пилот и пересобрать пакет. Без этого шаг дальше не идёт.

## Порядок (по одному сообщению, идемпотентно)
1. Повторно проверить production revision == revision из пакета (106). При расхождении — пересобрать пакет.
2. Повторно проверить content_hash артефакта == `08d2ed71976f61c9dcb332fac3751ade`. При расхождении — стоп.
3. Повторно проверить recipient (`betonomaster@yandex.ru`) и `email_source=manual_verified`.
4. Suppression check: opt-out / unsubscribe / bounce_suppressed == false.
5. One-time send gate: выдать единичный approval-токен под конкретный `lead_id:content_hash`, срок жизни короткий, одноразовый.
6. Отправить **ровно одно** SMTP-сообщение (без вложений, без трекинг-пикселя, без внешних ссылок).
7. Зафиксировать Message-ID от SMTP-сервера.
8. Append в send ledger: одна запись с реальным Message-ID, классификация COMMERCIAL.
9. При неопределённом ответе SMTP (нет подтверждения доставки) → пометить `uncertain`, поставить в quarantine, **не** повторять отправку.
10. Rollback невозможности повторной отправки: токен погашается сразу после попытки; повторная отправка по тому же `lead_id:content_hash` запрещена.
11. Включить reply monitoring (IMAP) по этому лиду.
12. **Никакого** автоматического follow-up: следующий контакт — только по отдельному решению владельца.

## Жёсткие инварианты на момент исполнения
- Ровно 1 SMTP-вызов, 1 ledger-запись, 0 follow-up, 0 payment.
- autosend остаётся BLOCKED для всех остальных лидов; sendAllowedLive включается точечно только под один токен и сразу выключается.
- Любая ошибка/неопределённость → quarantine, не retry.

**Этот план не исполняется сейчас. APPROVAL_TOKEN_ISSUED=NO, REAL_SEND_EXECUTED=NO.**
